#include "graphics.h"
#include "extgraph.h"
#include "genlib.h"
#include "simpio.h"
#include "conio.h"
#include <stdio.h>
#include <stdlib.h>
#include <stddef.h>

#include <windows.h>
#include <olectl.h>
#include <mmsystem.h>
#include <wingdi.h>
#include <ole2.h>
#include <ocidl.h>
#include <winuser.h>
#define maxline 100

/*static LineDB ReadLineDatabase(void){
	LineDB db;
	LineT lin;
	int nlines;
	db=New(LineDB);
	nlines=0;
	
}
static LineT ReadOneLine(void){
	
} */
/*typedef struct{
	double x,y;
}pointT; 
typedef struct {
	pointT a; 
	pointT b;
	double k;
	k=(a.y-b.y)/(a.x-b.x);
} *LineT;
typedef struct{
	LineT line[maxline];
	int nlines;
}*LineDB;
pointT CreatePoint(double x,double y){
	pointT p;
	p.x=x;
	p.y=y;
	return (p);
}*/ 

